## 1. The Checks count: weight and a live announcement

- [x] 1.1 In `packages/web/src/areas/studio/panels/ProcessTabRow.tsx`, add `fontWeight: 800` to the existing `tabCountBlocker` compiled style. That style carries `color: colors.refusal` alone today. The written face uses only weights 400 and 800, and `tabSelected` in this same file already reads 800. So no new weight enters the design language. Verify: the Checks tab's count renders bold only when `checksBlocked` is true. Every other tab's count keeps weight 400.
<!-- antislop: allow paragraph-length -->
<!-- A markdown blank line inside a checkbox bullet silently truncates that task's description in `openspec instructions`' own JSON output (confirmed by direct test against this repo); this bullet carries implementation detail that cannot move to a second paragraph. -->
- [x] 1.2 In the same file, add the transition announcement. Track the `checksBlocked` prop's previous value in a `useRef`. In a `useEffect`, set a local `announcement` state to `t("tabs.checksBlockingAnnounced")` only on the false-to-true edge. Return a fragment from `ProcessTabRow`. Render an always-mounted `<p {...stylex.props(styles.visuallyHidden)} role="status" aria-live="polite">{announcement}</p>` as a sibling, after the row's closing tag. Keep it outside the `role="tablist"` element, whose ARIA content model owns `tab` children alone. The region sits out of flow, so a fragment changes no layout and no caller. Mount it unconditionally. No engine reliably announces a live region that arrives with its text already inside it. `EntityTabs.tsx`'s own move-announcer is the local precedent for this pattern. Verify: the region sits in the DOM on a clear draft, holding empty text. It gains text only on the clear-to-blocker edge.
- [x] 1.3 Add `"tabs.checksBlockingAnnounced": "A blocking issue appeared in Checks."` to `packages/web/src/i18n/catalogs/studio.ts`, beside the existing `tabs.checksBlocking` key. This one is a full sentence, unlike the fragment `tabs.checksBlocking` holds. A live region reads on its own rather than joining an accessible name. Verify: `bun run typecheck` picks the key up through the `CatalogKey` union.

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 1.4 Extract the announcement edge into an exported `announcementAfter` helper, above the component. It takes the previous and current blocked flags plus the sentence. A rise returns the sentence. A stay-blocked returns `null`, which leaves the region alone. A fall returns the empty string. The effect writes whatever the helper returns, unless that is `null`. Emptying on the fall is load-bearing. Both edges write one constant, so a second rise would re-set identical text and mutate no text node. A browser pass measured that before the fix: the count recolored while a `MutationObserver` counted zero mutations. Emptying a polite region announces nothing itself, so the reverse transition stays silent. Verify: a rise, a fall and a second rise write three times, the sentence, the empty string, then the sentence.

## 2. Publish states a blocked draft's reason before the click

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 2.1 In `packages/web/src/areas/studio/screens/draftToolbarState.ts`, widen `PublishAvailability.reasonKey` to `"draftToolbar.publishUnavailable" | "draftToolbar.publishBlockedReason"`. Give `publishAvailability` a second parameter, `blocked: boolean`. Return the permission-denied reason when `canPublish !== true`. Return the blocked reason when `canPublish === true && blocked`. Return `{ available: true }` otherwise. The early return keeps the two reasons mutually exclusive by control flow, which drops the need for a priority rule. Verify: a blocked-but-permitted draft returns `available: true` carrying a `reasonKey`, a pair the old signature could not express.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 2.2 In `packages/web/src/areas/studio/panels/ProcessHeaderBar.tsx`, add a `blocked: boolean` prop to `PublishNavControl`, near line 288. Pass the existing `blocked` local into it at the call site, near line 786. That local already computes `checksDotState(groupChecksBySource(validation)) === "blocker"` for the confirm dialog. So this threads an existing value and does not add a second computation. Change the button's `aria-describedby` to key off `gate.reasonKey` instead of `gate.available`, so the new case's text reaches the button too. Leave `aria-disabled` and the `onClick` guard keyed off `gate.available`, unchanged: a blocked-but-permitted control stays operable and still opens the dialog. Rename `PUBLISH_REASON_ID`, near line 269, to `studio-publish-reason`, since after this change it also describes an available control. Rewrite that constant's doc comment at 267-268, and `PublishNavControl`'s own at 271-287. Both name the absent permission alone today, and this id now carries two reasons. Verify: the reason span renders in both cases, and only the permission case carries `aria-disabled`.
- [x] 2.3 In the same file, add a `publishBlockedReason` compiled style holding `color: colors.refusal` alone. Compose it after the existing `publishReason` style when `gate.reasonKey === "draftToolbar.publishBlockedReason"`. This mirrors how `tabCountBlocker` composes over `tabCount` in `ProcessTabRow.tsx`. The permission-denied reason keeps its muted tone, since it names an administrative fact rather than an issue in the draft. Add `"draftToolbar.publishBlockedReason": "Blocked by an open issue"` to `packages/web/src/i18n/catalogs/studio.ts`, matching the fragment style of its sibling key. Verify: `bun run typecheck` passes, and the two reasons render in different colors.

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 2.4 Take the reason line out of flow. Add a `publishGate` style holding `position: relative`. Put it on the `role="group"` wrapper. Give `publishReason` `position: absolute`, `insetBlockStart: "100%"`, `insetInlineEnd: 0`, a `space.s1` block margin and `whiteSpace: "nowrap"`. The trailing cluster is right-aligned behind `marginLeft: auto`, so anything in flow beside Publish pushes the cluster left. Task 8.6's design pass measured 167px at 1280px, plus a zero-pixel gap between the button and the text. An author reaching for Publish had it slide out from under the pointer, on a keystroke in another tab. Task 2.2's own docstring already claimed the line renders beneath the button, so this makes the code match it. Verify: Publish and Save hold their x positions across the clear-to-blocked edge. The line clears the tab row at 1280, 1024, 900, 768 and 375 pixels.

## 3. The structurally invalid banner

- [x] 3.1 In `packages/web/src/areas/studio/screens/EditScreen.tsx`, add `color: colors.refusal` to the `draftIncomplete` compiled style, near line 106. That style holds only margin resets today. Leave the banner's own condition, `!validation.zodValid`, and its `app.draftIncomplete` text unchanged. Do not reach for the `errorBanner`/`errorBannerStamp` pair in this same file. That bordered, rotated treatment stays reserved for a load or a request that comes back wrong. Verify: the banner renders in the refusal color, with no border and no stamp.

## 4. An issue's source renders as a label

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 4.1 In `packages/web/src/areas/studio/panels/shared/IssueList.tsx`, add an exported compiled style holding the label's visual identity. It reads `fontFamily: fonts.mono`, `fontSize: "0.8rem"`, `textTransform: "uppercase"`, `letterSpacing: "0.06em"` and `color: colors.textMuted`. Those are the exact values `ChecksRail.tsx`'s `checksGroupHeading` already holds, minus its block margins. In `IssueItems`, replace the inline `[{issue.source}] {issue.message}` text. Render a `<span>` carrying that style around `issue.source`, followed by the message. Keep the existing `issue issue-${issue.source}` literal className on the `<li>`, which another studio component also renders. No stylesheet reads either one, so this change keeps them rather than widening its scope. Verify: the rendered DOM has no bracket characters around the source, and the source sits in its own element.
- [x] 4.2 In `packages/web/src/areas/studio/panels/ChecksRail.tsx`, import that shared style and compose it under `checksGroupHeading`. Leave only the three margin properties in the local style. The `<h3>` then reads its face, size, transform, tracking and color from one shared definition. Rewrite the comment above that `<h3>`, which cites the bracket form task 4.1 removes. Verify: the grouped list's headings render exactly as before, and `bun run typecheck` passes for both files.

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 4.3 State the weight on `issueSourceLabel`. Add `fontWeight: 400` beside the four properties task 4.1 gave it. The rail composes the style onto an `<h3>`, whose user-agent default is bold. Task 8.6's design pass measured the same label twice. It read 700 in the rail and 400 in the per-entity list. One definition rendered two ways. This capability already asks both sites for one label treatment, and `design-language.md` gives a label weight 400. Verify: the rail's group headings and a per-entity list's source both report weight 400 in a real browser.

## 5. The tab row's roving-tabindex keyboard model

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 5.1 In `packages/web/src/areas/studio/panels/ProcessTabRow.tsx`, add the roving-tabindex keyboard model. Hold a `focusedTab` state, initialized to the `open` prop. Re-sync it whenever `open` changes, so a click keeps focus and selection together. Give each tab button `tabIndex={tab === focusedTab ? 0 : -1}`. Also give each one `onFocus={() => setFocusedTab(tab)}`. The roving anchor then follows real focus, whatever moved it: a click, an arrow key or a programmatic `.focus()`. Add an `onKeyDown` handler on the row. It answers `ArrowRight` and `ArrowLeft` by moving `focusedTab` one position through `PROCESS_TABS`, wrapping at both ends. It then calls `.focus()` on the newly focused button through a ref map. Let Enter and Space fall through to the button's existing `onClick`. That handler already calls `onOpen(tab)`, so activation stays where it is. Call `preventDefault()` on the two arrow keys alone, so the row's own sideways scroll does not also fire. Ignore a key event whose `target` is not one of the ten tab buttons. The row carries nothing else: its trailing edge is the last tab. The file `FieldMatrixGrid.tsx` is the local precedent for the ref map and the focus-moving handler. Group 6 lands the matching test changes, so the suite stays red between this task and 6.1. Verify: Tab from the header bar lands once on the open tab. A further Tab press leaves the row. The arrow keys move focus without opening a tab.
- [x] 5.2 In the same file, rewrite the module doc comment, near line 109. It states the opposite model today: every tab its own stop, with no roving tab stop and no arrow-key binding. State the roving-tabindex model instead. Name `spa-accessibility`'s own exception for a tab set of this shape. Verify: no comment in the file still describes the plain-button model as this row's own.

## 6. Tests

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 6.1 In `packages/web/test/studio-processTabRow.test.tsx`, cover what static markup can carry. These tests render through `renderToStaticMarkup` and mount no DOM, so none of them can move focus or fire a key. Assert the open tab renders `tabIndex={0}` and the other nine render `tabIndex={-1}`. Assert the rendered row carries exactly one tab stop, the open tab. Export `tabStops` from `packages/web/test/studio-fieldMatrixTabStops.test.tsx`, at line 56, and import it here. This is the suite's first cross-test-file import. Delete the existing `leaves every tab its own stop in the tab order` assertion, which asserts the markup has no `tabindex` at all. Rewrite the file's header comment, which states the plain-button model as this row's own. Arrow movement, the wrap and Enter activation all go to `docs/browser-checks.md` under task 7.2. That helper file already makes the same split and says so in its own header. Verify: copy the file outside this tree first. Revert task 5.1's `tabIndex` branch on that copy, and confirm both new assertions fail. Never mutate the shared tree for this.
- [x] 6.2 In the same file, assert the live region's static shape. It sits in the rendered markup with empty text on every static render, since `renderToStaticMarkup` runs no effect. That is the whole of what a markup test sees here. The clear-to-blocker announcement itself needs a real assistive technology, so it goes to `docs/browser-checks.md` under task 7.2. The count's bold weight goes there too. A compiled class hashes, so no markup assertion can name a weight. The existing class-difference assertion already passes on the color alone. Verify: the live region stands, and stays empty, in the static markup for both a clear and a blocked draft.
- [x] 6.3 In `packages/web/test/studio-draftToolbarState.test.ts`, cover `publishAvailability`'s new second parameter across all four input pairs. Assert a permitted, unblocked draft returns `{ available: true }` with no `reasonKey`. Assert a permitted, blocked draft returns `available: true` carrying the blocked reason. Assert an unpermitted draft returns the permission reason whatever `blocked` holds. In `packages/web/test/studio-processHeaderBar-publishGate.test.tsx`, assert the blocked-but-permitted control renders the reason text and references it through `aria-describedby`. Assert it has no `aria-disabled`. Task 2.2's new required prop breaks five call sites here: lines 294, 304, 312, 323 and 332. Pass `blocked={false}` at each, and `blocked: false` at line 323's object-call form, which keeps every existing verdict. Verify: `bun run typecheck` passes for both test files.
- [x] 6.4 Add an assertion covering `IssueItems`' new rendering. The source renders inside its own element, and the rendered text has no bracket characters around it. Grep `packages/web/test/**` first for any existing assertion matching the old `[source] message` text. Change whatever that grep finds, in the same pass. Verify: the grep returns no remaining assertion against the bracket format.
- [x] 6.5 Run the affected test files directly for fast feedback, with `DATABASE_URL` set: `bun test packages/web/test/studio-processTabRow.test.tsx packages/web/test/studio-processHeaderBar-publishGate.test.tsx packages/web/test/studio-draftToolbarState.test.ts`. Add whichever file task 6.4 lands in. Verify: every listed file passes before the full-suite run in Verification.

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 6.6 Cover task 1.4's edge in `packages/web/test/studio-processTabRow.test.tsx`. No test here can fire the effect, since this repo ships no DOM test library. Task 1.4 exports `announcementAfter` for that reason, and this file tests the helper. The file `studio-draftProvider-chainingFetch.test.ts` states the same convention and names the same reason. Assert all four inputs. The rise carries the sentence. A stay-blocked returns `null`. The fall returns the empty string. A replayed rise, fall and rise ends holding the sentence. Verify: revert 1.4's fall branch on a copy outside this tree. Confirm the last two assertions fail. Never mutate the shared tree for this.

## 7. Documentation

- [x] 7.1 Grep `docs/`, `.claude/rules/` and `packages/web/src/**` for passages describing any of the three changed behaviors. Those are the tab row's keyboard model, the `[source]` bracket format, and the uncolored draft-incomplete banner. The file `docs/current-state.md` names exported symbols by hand and is the likely holder. Change whatever the grep finds, in this same change. Verify: no remaining passage describes the plain-button tab model or the bracket-prefixed issue format as current.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 7.2 Add five entries to `docs/browser-checks.md`, one per fix, carrying seven Pass lines between them. `development-toolchain`'s split rule applies to each. This repo has produced none of these five defects before. Several cases need real tooling to judge, since the harness mounts no DOM. Name each one explicitly. Four of those belong to the tab row. Those are the count's bold weight, the clear-to-blocker announcement, arrow-key focus movement with its wrap, and Enter activation. Three more cover the pre-click Publish reason, the banner color, and the issue label. A browser or an assistive technology judges each. Verify: every Pass line above appears under one of the five entries.

## 8. Verification

- [x] 8.1 Run `bun run typecheck`, then `bun run build`. Report what each command printed.
- [x] 8.2 Run the full `bun test` suite with `DATABASE_URL` set. Never use a single-file rerun for the final signal. Report the pass count and the skip count. Confirm the skip count is not silently elevated (`scripts/gates/silent-green.sh` reads this from the captured output).
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 8.3 Run the antislop prose gate over this change's new and changed Markdown: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/prose.sh`. If the range is empty pre-commit, check the files directly instead. Those are this change's `proposal.md`, `design.md` and `tasks.md`, its four `specs/**/spec.md` delta files, plus `docs/browser-checks.md` and whatever task 7.1 touched. This is a per-file ratchet: only a rise in a file's own finding count matters. Resolve every finding.
- [x] 8.4 Run the whitespace gate over the same range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/whitespace.sh`.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 8.5 Run every entry task 7.2 added to `docs/browser-checks.md`, against a real browser and a production build. Confirm every "Pass:" line. Include the dark-scheme repeat for both new colors. This ran against the container's production bundle, on the derived port. Every Pass line held except the announcement's repeat, which task 1.4 fixes. One entry named a gesture the Paths tab does not carry. That tab is a read-only overview and introduces no blocker, so `docs/browser-checks.md` now names the Canvas palette. The screen reader itself stayed unrun, since this host has no assistive technology. A `MutationObserver` on the live region stood in for it. Measured: the blocked count reads `rgb(174, 24, 0)` at weight 800 in light. Dark reads `rgb(255, 151, 131)`, 7.9:1 against its ground. The banner and the blocked publish reason read those same two values. The permission reason reads `rgb(96, 93, 93)` and keeps `aria-disabled`. The tab order runs Publish, the `⋮` trigger, the open tab, then out.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 8.6 Run `/impeccable critique` and `/impeccable audit` against the studio process-surface route. This repo's mandatory design-workflow step requires both, for any `packages/web` UI change. Resolve any finding either report raises against this change's own five fixes. A finding about something this change did not touch belongs in its own change instead. The critique ran dual-agent, one isolated agent per assessment, as its own reference demands. The detector reports zero anti-patterns over `areas/studio` and 28 advisory notes, all one rule, `design-system-font-size`. Only `IssueList.tsx:18` is new, and it carries the value `ChecksRail.tsx` already held. Two findings landed on this change's own fixes, and tasks 2.4 and 4.3 resolve them. Three landed outside it. The refusal color marks five held-back groups in `ChecksRail.tsx:48`, above a plain-text failing check. A visually hidden span in the tab row overflows the document by 5 pixels below 1010 pixels. The row's 40-pixel tabs sit under the 44-pixel touch target. Commit `e46ad030` carries the first two, ahead of this change.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.2 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 8.7 Run the antislop linter on each of the four live specs after the specs sync at archive time. Compare each count against its pre-sync count and repair any rise. A delta's own vocabulary lands in a whole file at sync time, and `synonym-rotation` re-scores prose the delta never touched. One measured example exists. This change's uses of "blocking issue" tip a `studio-publish` finding onto base prose the delta leaves alone. Where the offending line is base prose, add a targeted `<!-- antislop: allow synonym-rotation -->` with a one-line reason above it. Do not reword the delta for a base-prose finding. Verify: each of the four live specs reports a count at or below its pre-sync count. Measured across the sync. `spa-accessibility` went 28 to 27, `studio-checks-rail` held at 7, and `studio-process-tabs` held at 2. `studio-publish` rose by one, exactly as this task predicted. The rise landed on base prose the delta never touched, about how the engine turns away an unauthorized caller. A targeted `synonym-rotation` directive above that line returns the file to its pre-sync count. The delta's own text stayed as written.
