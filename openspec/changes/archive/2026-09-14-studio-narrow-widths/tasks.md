## 1. Worktree setup

- [x] 1.1 Copy `.claude/skills/impeccable` from the main checkout into this worktree. Revert the tracked `.impeccable/config.json` wherever the copy touched it. Verify that a PostToolUse payload naming a `packages/web` file, piped into `hook.mjs`, prints findings.

## 2. The shell header

- [x] 2.1 Write `packages/web/test/shell-headerWrap.test.ts` first, over the source text of `Chrome.tsx` and `navStyles.ts`. Declare a `stripComments` helper in it, as `studio-guidedSurfaceStyle.test.ts` does. Match every check against source with its comments stripped. Its first check expects `flexWrap: "wrap"` in the `header` block, and no `30rem` there. Verify the check fails against today's code.
- [x] 2.2 Add three checks to the same file first, per the Tests list in `design.md`. The `accountGroup` block declares `flexGrow: 1` and no `marginLeft`. The `accountName` block declares `contain: "inline-size"`, `flexGrow: 1`, `textAlign: "end"` and `minWidth: "6rem"`. The `nav` block declares no `flex` shorthand and keeps both 30rem rules. Verify every assertion fails against today's code, except the two 30rem rules and `minWidth: "6rem"`.
- [x] 2.3 Add a case to `chrome-header.test.tsx` first. The `title` stands on an inner span that holds the actor's name alone. Verify the case fails against today's code.
- [x] 2.4 Change the `header`, `accountGroup` and `accountName` styles in `Chrome.tsx`, per `design.md`. Move the identity span's `title` onto an inner span. Rewrite the doc comment above the styles, which names one narrow-viewport rule. Rewrite the `nav` prop's doc comment, which names the one header row. Verify typecheck passes.
- [x] 2.5 Drop `flex: 1` from the `nav` style in `navStyles.ts`. Add no `flexWrap` there. Rewrite its doc comment, which names the sub-30rem collapse alone. Verify 2.1 to 2.3 pass in the full suite run.
- [x] 2.6 Compile the bundle with `bun run build` in the devcontainer. Read the compiled stylesheet under `packages/web/dist/assets/`. It prints a space after each colon, so search it with `/contain:\s*inline-size/`. Verify that search finds a rule. Where it finds none, fall back to a literal residual rule per `web-styling`, before group 3. The header's wrap reuses an atom other styles already emit, so the browser check in 6.4 confirms it.

## 3. The form editor

- [x] 3.1 Write `packages/web/test/studio-narrowWidths.test.ts` first, over source text with comments stripped. Declare its own `stripComments` helper, as `shell-headerWrap.test.ts` does. Both `FormEditorScreen.tsx` and `FormPreview.tsx` declare `PREVIEW_NARROW` as `"@media (max-width: 80rem)"`. Neither file names `64rem`. Verify the test fails against today's code.
- [x] 3.2 Raise `PREVIEW_NARROW` to 80rem in `FormEditorScreen.tsx`. Rename `NARROW` to `PREVIEW_NARROW` in `FormPreview.tsx`, at the same value. Rewrite the two comments in `FormEditorScreen.tsx` that tie the turn to the step page and the entity tabs. Verify typecheck passes, and 3.1 passes in the full suite run.

## 4. The tab row

- [x] 4.1 Add `fadeState` and `tabRestsInView` cases to `studio-processTabRow.test.tsx` first, through the namespace import `import * as tabRow`. For `fadeState`, a row with no overflow reads `"none"`, its start `"end"`, mid-scroll `"both"` and its end `"start"`. At `scrollLeft` 1 it still reads `"end"`, and one pixel short of its end it still reads `"start"`. For `tabRestsInView` at 32px padding, a tab mid-row reads `true` at 32px and 31px clear, `false` at 20px. The first tab flush at `scrollLeft` 0 reads `true`, as does the last tab 0.5px short of the end limit. Verify each case fails against today's code.
- [x] 4.2 Add checks to `studio-narrowWidths.test.ts` first. The row block declares `scrollPaddingInline: space.s8`, a `maskSize` naming `--tab-row-band`, and `maskRepeat: "no-repeat"`. Each of `fadeStart`, `fadeEnd` and `fadeBoth` sets `maskImage` to `"none"` under forced colors. The file `ProcessTabRow.tsx` declares no `transition` and no `animation`. Verify the padding and mask checks fail against today's code.
- [x] 4.3 Add a one-mechanism check to the same file first, over source with comments stripped. The file `EditScreen.tsx` calls `scrollTabIntoRow` and names no `scrollIntoView`. The file `ProcessTabRow.tsx` names `scrollIntoView` once, with `inline: "nearest"` and `behavior: "instant"`. Verify the check fails against today's code.
- [x] 4.4 Export `fadeState` and `tabRestsInView` from `ProcessTabRow.tsx`, per `design.md`. Add the row's scroll padding, its mask size, position and repeat, and the three fade styles. Verify typecheck passes, and 4.1 and 4.2 pass in the full suite run.
- [x] 4.5 Compile the bundle with `bun run build` in the devcontainer. Read the compiled stylesheet under `packages/web/dist/assets/`. Search each of `scroll-padding-inline`, `mask-size`, `mask-position` and `mask-repeat` as `name:\s*`. Verify at least three rules match `/mask-image:\s*linear-gradient\(/`. Verify `/mask-image:\s*none/` matches inside an `@media (forced-colors: active)` block. Where one search finds nothing, fall back to a literal residual rule per `web-styling`, before 4.6.
- [x] 4.6 Export `scrollTabIntoRow` from `ProcessTabRow.tsx`. Call it from a layout effect on `open` and from the arrow-key handler. Pass `{ preventScroll: true }` to the arrow-key handler's `focus()`. Call it from each tab's `onFocus` as well, when the button matches `:focus-visible`. Verify the roving `tabindex` and the `preventDefault` stand as before.
- [x] 4.7 Wire the `onScroll` handler and one `ResizeObserver` over the row and its ten buttons, per `design.md`. The observer's callback writes `--tab-row-band` and the fade value. Have the layout effect on `open` write the open tab's button to a ref. The callback reads the open tab through that ref. Verify typecheck passes.
- [x] 4.8 Have the `onScroll` handler record whether the open tab rests in view, through `tabRestsInView`. Record the same after each call the component makes to `scrollTabIntoRow`, whether or not the row moved. The observer's callback calls `scrollTabIntoRow` when the row's own width moved. It also calls it when a button's width moved while that record reads true. Once the callback has acted, have it record the same again. Verify typecheck passes.
- [x] 4.9 Replace the `scrollIntoView` line in the `focusTabButton` of `EditScreen.tsx` with a call to `scrollTabIntoRow`. Pass `{ preventScroll: true }` to its `focus()`. Rewrite its doc comment to name the shared function. Verify 4.3 passes in the full suite run.

## 5. Documentation

- [x] 5.1 Delete the FORMS-11, FORMS-12 and FORMS-16 bullets from the Forms tab audits section of `docs/decisions.md`. Verify `grep -nE 'FORMS-(11|12|16)\b' docs/decisions.md` prints nothing.
- [x] 5.2 Move FORMS-15 into the `Decided, not yet built` section, as the entry that `design.md` quotes under G2. Place it as that section's last top-level bullet, after "Five literal `#726e6e` sites want the new `colors.dormant` role". Verify `grep -n 'FORMS-15' docs/decisions.md` prints that entry's line alone.
- [x] 5.3 Count the top-level bullets left under the heading "Open from the 2026-09-13 Forms tab audits". Where none stands, delete that heading and its opening paragraphs. Where a bullet stands, keep both and name each such bullet in the ledger. Verify every other section heading of the file stays.
- [x] 5.4 Add the section that `design.md` quotes under "The area nav keeps one line", directly above `Refused simplifications`. Cite the `nav` block in NAV-1 by file and line, as it stands after group 2. Verify `grep -n 'NAV-1' docs/decisions.md` prints the bullet's line alone.
- [x] 5.5 In the Layout list of `DESIGN.md`, rewrite the 30rem line to name the nav's own line. Add an 80rem line for the form editor's preview. Verify the list reads 30rem, 40rem, 64rem, 80rem and the container queries, in that order.
- [x] 5.6 Extend the Header bullet of the Navigation section in `DESIGN.md` with the wrap and the identity span's floor. Extend the Row bullet of its Tab Row section with the scroll and the edge fade. Verify the first names the 12px line gap and the second the 24px fade.
- [x] 5.7 Rewrite the tab row paragraph in the `docs/current-state.md` entry that names `panels/ProcessTabRow.tsx`. Name `fadeState`, `tabRestsInView`, `scrollTabIntoRow` and `--tab-row-band` in it. Rewrite the hand-off paragraph of the same entry to name `scrollTabIntoRow`. Verify each symbol exists after group 4.
- [x] 5.8 Add one sentence on the 80rem turn to the form preview paragraph of the same entry. Verify the sentence names both files that declare `PREVIEW_NARROW`.
- [x] 5.9 Add an *edge fade* row beside the tab row in the glossary table of `.claude/rules/ui-glossary.md`. In the form preview row, replace "right pane" with "trailing pane, under the canvas at 80rem and below". In the table row for *header*, drop "the one fixed row". State there that the header wraps where its items run out of room. In the paragraph above the table, replace "the same header row" with "the same header". Verify the file names the fade by that one term, and no longer calls the header one fixed row.
- [x] 5.10 In `docs/browser-checks.md`, rewrite the 30rem paragraph of the StyleX pilot entry to point at the new header entry. Verify the paragraph names no 30rem wrap condition.
- [x] 5.11 Open the entry "Tabbed step form: the editor's strip, the preview pane and the participant" in `docs/browser-checks.md`. In its paragraph "The preview pane matches the canvas", name a window wider than 1280px. Verify that paragraph states the width where the pane stands beside the canvas.
- [x] 5.12 Append a header entry to `docs/browser-checks.md`, with `studio-narrow-widths` as its source. Its step 1 signs in as `demo-superuser@example.test` in the studio. The step reads the header's own `scrollWidth` against its `clientWidth` at 1440, 680, 640, 600, 560, 480 and 400px. The document's width counts at 560px alone, since the process list table overflows at 480px and below. Verify the step names every width.
- [x] 5.13 Give step 1 its first pass lines. Pass: at 1440px the header holds one line, and the identity span shows the whole name. Pass: no width clips a header item, and 680px holds one line with the name cut to an ellipsis. The tester records the width where the account group first leaves the first line, expected between 600 and 650px. Verify the step states each pass line.
- [x] 5.14 Give step 1 its remaining pass lines, at 560px and 400px. Pass: at 560px the document's `scrollWidth` equals its `clientWidth`. Pass: at 560px the muted background and the 2px divider span both lines, with the divider under the second. Pass: at 400px the register tab and the account group share the first line. Verify the step states each pass line.
- [x] 5.15 Append step 2 to the header entry. At 1440px the tester reads `document.elementFromPoint(x, y).closest('[title]')` over the free header room, then over the name. Pass: it reads `null` over the free room, and the inner span that holds the name over the name. Verify the step states its pass line.
- [x] 5.16 Append step 3 to the header entry. At 560px the tester tabs through the header. Pass: focus reaches the nav's buttons, then the Account button, in the order it takes at 1440px. Verify the step states its pass line.
- [x] 5.17 Append step 4, which repeats step 1's widths in the app, admin and reporting areas. Reporting opens on a process's Cycle time view. The tester signs in as an actor holding every role, in English and then in German. Pass: the Account button's box stands inside the header's box at every width. The tester records each width where a nav still overflows, and where each header first takes a second line. Verify the step names both locales.
- [x] 5.18 Append a form editor entry, with `studio-narrow-widths` as its source. Its steps open "Submit the Exit Notification" from a draft of `it_offboarding`, at 1300, 1280 and 1100px. Each step compares the height of the `full_name` key's box with its computed line height. Verify the entry names each width.
- [x] 5.19 Give those steps their pass lines. At 1300px three columns stand, with the preview beside the canvas. At 1280px and 1100px the preview stands under both columns, its 2px divider on its top edge alone. At all three widths the key holds one line. Verify the entry states each pass line.
- [x] 5.20 Append a tab row entry, with `studio-narrow-widths` as its source. Step 1 opens a draft at `/studio/processes/<id>/edit/forms` in a 400x800 window. Pass: the Forms tab's box stands inside the row's box, 32px or more from each edge, to within one pixel. Verify the step states its pass line.
- [x] 5.21 Append step 2, which moves the row's `scrollLeft` to its start, its middle and its end. After each move the tester waits two animation frames, then reads the row's computed `mask-image`. Pass: a trailing gradient alone, then both, then a leading gradient alone. Verify the step states its pass lines.
- [x] 5.22 Append step 3, which opens the draft at `/edit/canvas` in a 1440px window. Pass: the row's `mask-image` reads `none`. The tester opens Forms with a click, then narrows the window to 400px. Pass: the Forms tab stands whole in view. Verify the step states both pass lines.
- [x] 5.23 Append step 4, on a draft that differs from its base version. The tester opens `/studio/processes/<id>/edit/checks` in a 400x800 window. The tester waits until the Changes tab's count prints. Pass: the Checks tab stands whole in view, with the row at its end. Verify the step states its pass line.
- [x] 5.24 Append step 5, where the tester opens `/edit/forms` at 400px, then opens Canvas. The tester then presses the browser's Back button. Pass: Forms opens again and stands whole in view, and the row jumps with no animated scroll. Verify the step states its pass line.
- [x] 5.25 Append step 6, a keyboard walk. The tester focuses Forms and presses the right arrow key three times. Pass: focus stands on Changes, whole in view and 32px clear of the edge, to within one pixel. Verify the step states its pass line.
- [x] 5.26 Append step 7, a pointer press through `run-code`. The tester scrolls the row to its start and waits two animation frames. The tester presses 12px inside the row's trailing edge, where `elementFromPoint` reads Data sources, with `page.mouse.down()` and `page.mouse.up()`. Pass: the row's `scrollLeft`, read between down and up, equals its value before the press. Pass: Data sources then opens and stands whole in view. Verify the step states both pass lines.
- [x] 5.27 Append step 8 at 400px. The tester sets the row's `scrollLeft` to 0 and waits two animation frames. The tester focuses Fields and presses Enter. Pass: `scrollLeft` still reads 0, and Fields is open. Verify the step states its pass line.
- [x] 5.28 Append step 9, which needs a classic scrollbar under the row. Headed Chrome on Windows draws one, as the MATRIX-1 run did. The tester zooms a screenshot of each faded edge. Pass: the scrollbar's arrows and the 2px divider keep full strength under each fade. Verify the step names the headed session it needs.
- [x] 5.29 Append step 10, which emulates forced colors through `run-code` and `page.emulateMedia`, then clears the emulation. Pass: the row's `mask-image` reads `none` at 400px. Verify the step states its pass line.
- [x] 5.30 Append step 11 at 400px, with Forms open. The tester sets the row's `scrollLeft` to the Forms button's `offsetLeft + offsetWidth`, minus the row's `clientWidth`, minus 8. Forms then stands 8px past the trailing edge, and the tester waits two animation frames. The tester focuses the header bar's `⋮` menu button, then presses Tab. Pass: focus stands on Forms, whole in view and 32px clear of the edge, to within one pixel. Verify the step states its pass line.
- [x] 5.31 Append step 12 in a 400x800 window. The tester opens `/edit/forms`, sets the row's `scrollLeft` to 0, waits two animation frames, then clicks Fields. Through `run-code`, the tester sets the Steps button's `style.minWidth` to its `offsetWidth` plus the row's `clientWidth`. The tester then waits two animation frames. Pass: the Fields tab stands whole in view and 32px clear of the edge, to within one pixel. Verify the step states its pass line.
- [x] 5.32 Extend the entry "Keyboard focus hand-off from a Checks row and a Changes row" with a 400px step. The tester scrolls the tab row to its end before pressing the open command. Pass: focus lands on Fields, whole in view and 32px clear of the edge, to within one pixel. Add `studio-narrow-widths` to the entry's Source line. Verify the step states its pass line.
- [x] 5.33 Re-read each citation in `docs/decisions.md` that names `ProcessTabRow.tsx:`, `EditScreen.tsx:`, `Chrome.tsx:`, `navStyles.ts:`, `FormEditorScreen.tsx:`, `FormPreview.tsx:`, `browser-checks.md:`, `current-state.md:`, `DESIGN.md:` or `ui-glossary.md:`. Include each bare `:NNN` continuation after one. Correct each line this change moved. Verify each corrected citation matches its line.

## 6. Verification

- [x] 6.1 Run `bun run typecheck`, then `bun run build`, in the devcontainer. Verify both exit 0. Record what each printed.
- [x] 6.2 Run the full `bun test` with `DATABASE_URL` set, piped through `scripts/gates/silent-green.sh`. Verify zero failures and a skip count at or under the floor.
- [x] 6.3 Run `sh ~/.claude/skills/impeccable/scripts/impeccable detect --json` once on each component file this change touched. The six are `Chrome.tsx`, `navStyles.ts`, `ProcessTabRow.tsx`, `EditScreen.tsx`, `FormEditorScreen.tsx` and `FormPreview.tsx`. Verify no finding stays open, or note why one stands.
- [x] 6.4 Open the session with `playwright-cli -s=studio-narrow-widths open --headed`, so the row draws a classic scrollbar. Run the three new browser-check entries and the 400px hand-off step on the production bundle. Pass `-s=studio-narrow-widths` to every later `playwright-cli` call. Read the DOM through `run-code` with `page.evaluate`. Verify every step passes, and note each threshold width the header entry finds.
- [x] 6.5 Run `/impeccable critique` on `/studio/processes/<id>/edit/forms` at 400px and at 1440px. Fix each finding, or note it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [x] 6.6 Run `/impeccable audit` on the same route at the same widths. Fix each finding, or note it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [x] 6.7 Run `/impeccable critique` on the form editor for "Submit the Exit Notification" at 1100px and at 1300px. Fix each finding, or note it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [x] 6.8 Run `/impeccable audit` on the same form editor route at the same widths. Fix each finding, or note it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [x] 6.9 Repeat task 5.33 wherever a fix in 6.3 to 6.8 touched a cited file. Verify each re-read citation matches its line.
- [x] 6.10 Run the prose gate over the pushed range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/prose.sh`. Verify it exits 0.
- [x] 6.11 Run the whitespace gate over the same range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/whitespace.sh`. Verify it exits 0.
