## 1. Studio component changes

<!-- antislop: allow synonym-rotation -->
<!-- "Discard" below names only the fixed "Discard draft" button label, never a word choice competing with "delete"/"remove"; every occurrence in this file pairs it with "draft". -->
- [x] 1.1 In `packages/web/src/areas/studio/panels/ChecksRail.tsx`, delete the `collapsed`/`onOpen` props and the collapsed rendering branch. Also delete the `dot`/`summary`/`summaryName` local computations that only that branch reads; they sit above the JSX, never inside it. Delete the now-unused `checksDotState`/`totalOpenIssueCount` imports too, and the `checksRailBare`/`checksRailSummary`/`checksRailDot*`/`checksRailSummaryCount` compiled styles. Keep `groups`/`clear`/`allChecksClear`/`groupChecksBySource`: the full list still reads them. Verify: the component renders only the full grouped-by-source form, and `bun run typecheck` reports no new error in this file.
- [x] 1.2 In `packages/web/src/areas/studio/panels/DraftNavControls.tsx`, drop the `ChecksRail` import and mount and the `onOpenChecks` prop. Keep the existing `checksDotState`/`groupChecksBySource` import and `blocked` computation: the publish-confirm dialog still reads it (`blocked={blocked}` on `PublishConfirmDialog`). Verify: the area nav renders exactly Save, Discard draft and Publish. Also verify that `bun run typecheck` reports no new error in this file.
<!-- antislop: allow paragraph-length -->
<!-- A markdown blank line inside a checkbox bullet silently truncates that task's description in `openspec instructions`' own JSON output (confirmed by direct test against this file); this bullet carries real verification detail that cannot move to a second paragraph. -->
- [x] 1.3 In `packages/web/src/areas/studio/panels/ProcessTabRow.tsx`, add a `checksBlocked: boolean` prop. Add a `tabCountBlocker` compiled style that reuses the `colors.refusal` token. That token is the same one `ChecksRail`'s blocker dot and `checksGroupHeldBack` already read, so no new color enters the palette. Apply it to the Checks tab's count span only, when `tab === "checks" && checksBlocked`. Color alone reaches no screen reader (WCAG 1.4.1). The removed dot paired its color with a full accessible-name sentence, so this cannot regress that. Add a `visuallyHidden` compiled style too, copying `EntityTabs.tsx`'s own move-announcer pattern. Render a visually-hidden `<span>{t("tabs.checksBlocking")}</span>` inside the Checks tab's button when blocked, plus the new `"tabs.checksBlocking": "blocking a publish"` catalog key. Verify: a Storybook-free manual read of the diff shows every other tab's count style untouched. The blocked Checks tab's accessible name, visible text plus hidden span, states the blocking fact in words.
- [x] 1.4 In `packages/web/src/areas/studio/screens/EditScreen.tsx`, add a new import of `checksDotState`/`groupChecksBySource` from `../draft/checksRail.js` (this file imports nothing from there today, confirmed by grep). Compute `checksBlocked` (`checksDotState(groupChecksBySource(validation)) === "blocker"`) beside the existing `processTabCounts(...)` call. Pass it into `ProcessTabRow`, and drop the `onOpenChecks` prop passed into `DraftNavControls`. Verify: `bun run typecheck` passes for this file.
- [x] 1.5 Grep the repo for each of `checksRail.summaryHeldBack`, `summaryClear`, `summaryBlockerOne`, `summaryAdvisoryOne`, `summaryBlocker`, `summaryAdvisory` and remove any now-unreferenced entry from `packages/web/src/areas/studio/catalog.ts`. Verify: the grep for each removed key returns no remaining call site. `bun run typecheck` still passes; a stale `CatalogKey` reference would fail it.
- [x] 1.6 Found during implementation, not in the original plan: removing 1.1's `summary = totalOpenIssueCount(groups)` call orphans `totalOpenIssueCount`/`OpenIssueSummary` in `draft/checksRail.ts`. Grep confirms zero remaining callers anywhere in `packages/web/src`. Delete both, and the doc comments naming "the collapsed checks summary"/"the collapsed-summary requirement" on `totalOpenIssueCount` and on `checksDotState`. Verify: grep for `totalOpenIssueCount` and `OpenIssueSummary` returns no hit outside their own removal diff.

## 2. Tests

<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.3 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 2.1 Grep `packages/web/test/**` for `collapsed`, `onOpenChecks`, and each catalog key removed in 1.5. Change or delete every assertion that targets the removed area-nav Checks control. Expect `studio-draftNavControls.test.tsx` and `studio-checksRail.test.ts` to need changes, but confirm the full list from the grep rather than assuming it. Separately, `bun run typecheck` (run once after group 1) also flags `studio-processTabRow.test.tsx`'s single `render()` helper: it is missing the new required `checksBlocked` prop. Add it there (default `false`) regardless of what the grep finds. That prop is new; nothing removed it. Verify: no remaining reference to the removed control or props in `packages/web/test/**`, and `bun run typecheck` reports no error under `packages/web/test/`.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.3 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 2.2 No `bun:test` assertion covers the count's color itself. `development-toolchain`'s "A browser check lands as an assertion or as a checklist entry" requirement applies here. It keeps a check manual unless this repository already produced the error it catches. No prior error record exists for a feature this change introduces. Instead, add a `checksBlocked` case to `studio-processTabRow.test.tsx`'s existing `render()` helper (task 2.1 already touches this file). Assert the *structural* fact a browser can't render but a static-markup test can. The Checks tab's count span carries a different `className` when `checksBlocked` is `true` versus `false`. Every other tab's count span's class stays identical between the two renders. The color itself belongs to `docs/browser-checks.md`'s new "The Checks tab's colored count" entry (added in this change), covered at task 4.5. Verify: the new assertion fails when `tabCountBlocker` applies to the wrong tab, or applies unconditionally. It passes as implemented.
- [x] 2.3 Run the affected test files directly for fast feedback: `bun test packages/web/test/studio-draftNavControls.test.tsx packages/web/test/studio-checksRail.test.ts packages/web/test/studio-checksRail-publishVerdict.test.tsx packages/web/test/studio-checksRail-narrowing.test.tsx packages/web/test/studio-processSurface.test.ts` with `DATABASE_URL` set. Verify: every listed file passes before moving to the full-suite run in Verification.

## 3. Documentation

- [x] 3.1 Change `.claude/rules/ui-glossary.md`'s process-surface section. "Checks, Save, Discard draft and Publish stand in the area nav" becomes an empty area nav. The three now stand in the header bar. The "dock" paragraph's "two forms" of the checks rail becomes one form: the full grouped list on the Checks tab. Verify: no remaining sentence in that file claims a fourth area-nav control or a collapsed rail form.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.3 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 3.2 Directly change `openspec/specs/studio-checks-rail/spec.md`'s `## Purpose` section. Do this instead of via this change's delta. Per this repo's OpenSpec convention, an existing capability's Purpose changes on the live spec directly. Drop the "Both forms"/"two forms" framing now that only the full grouped list remains. Leave the pre-existing "third column"/"inspector's bottom edge" architecture description alone. This change did not cause that drift. The drift predates it and stands apart from it. Verify: the Purpose section no longer says "both forms."
<!-- antislop: allow sentence-length -->
<!-- Linter miscount: the sentence naming the five files reads under 20 words on its own (verified in isolation); it only reports long once the later quoted doc-comments, several lines further into this same bullet, are also present. -->
- [x] 3.3 Change the doc comments that currently describe the removed control and the plain, uncolored tab count as intended design. Five files carry them. They are `DraftNavControls.tsx`, `ChecksRail.tsx`, `draft/process-tabs.ts`, `root.tsx`, and `ProcessHeaderBar.tsx`. `root.tsx` carries the `navSlot` comment: "the studio's four draft controls render into: Checks, Save, Discard draft and Publish." `ProcessHeaderBar.tsx` carries the comment: "Those four stand in the studio's area nav, beside Checks." Make every comment match the code after this change. Verify: re-read each file's module- and function-level comments after 1.1-1.4 land. Confirm none still describes the removed collapsed control as still there, or names "four" controls.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.3 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 3.4 Rewrite `docs/authoring-guide.md:779-782` ("Studio's own area nav carries **Checks**, **Save**, **Discard draft** and **Publish**. Those four stand there whatever you have open. Checks reads as a dot and a count. The dot says whether anything blocks a publish, and pressing it lists what Studio found."). Describe three area-nav controls instead. Move the dot/blocker behavior to the Checks tab's own count, in the tab-row paragraph directly below it (lines 784-786). Verify: no remaining sentence in the file says "those four" or describes a dot-and-count area-nav Checks control.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.3 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 3.5 Change `docs/current-state.md:4396-4400` ("The component `DraftNavControls.tsx` renders Checks, Save, Discard draft and Publish into the studio's area nav... The function `checksDotState` in `draft/checksRail.ts` decides the Checks dot: blocker, advisory or clear."). Match it to the three-control nav and the tab row's colored count. This file's staleness is advisory per CLAUDE.md. It is not gated. This change still touches the passage directly. Verify: the passage no longer claims `DraftNavControls` renders a Checks control.

## 4. Verification

- [x] 4.1 Run `bun run typecheck`, then `bun run build`. Report what each command printed.
- [x] 4.2 Run the full `bun test` suite with `DATABASE_URL` set. Never use a single-file rerun for the final signal. Report the pass count and the skip count. Confirm the skip count is not silently elevated (`scripts/gates/silent-green.sh` reads this from the captured output).
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.3 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [x] 4.3 Run the antislop prose gate over this change's new and changed Markdown: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/prose.sh`. If the range is empty pre-commit, check nine files directly instead: `openspec/changes/checks-status-consolidation/proposal.md`, `design.md`, `tasks.md`, both `specs/**/spec.md` delta files, `.claude/rules/ui-glossary.md`, `docs/authoring-guide.md`, `docs/current-state.md`, and `docs/browser-checks.md`. For the last three, check only the paragraphs or section this change added or changed. This is a per-file ratchet: only a rise in a file's own finding count matters, never its pre-existing debt. Resolve every finding. Where a finding comes from verbatim-copied requirement text in a delta spec, the fix is different. Add a targeted `<!-- antislop: allow <rule> -->` with a one-line reason instead. This repo has a known trap here: "a new delta file has base 0." That exception does not cover new prose this change wrote.
- [x] 4.4 Run the whitespace gate over the same range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/whitespace.sh`.
- [x] 4.5 Run `docs/browser-checks.md`'s new "The Checks tab's colored count" entry (added by this change). `development-toolchain`'s split rule applies. No prior error record exists for this new visual behavior, so it stays manual rather than becoming a `bun:test`. Confirm every "Pass:" line in it. That includes the dark-scheme repeat and the advisory-only case.
<!-- antislop: allow paragraph-length -->
<!-- Same trap as 1.3 above: a paragraph break inside this checkbox bullet truncates the task in `openspec instructions`' JSON output, so this stays one paragraph. -->
- [ ] 4.6 Run `/impeccable critique` and `/impeccable audit` against the studio process-surface route. This repo's mandatory design-workflow step requires both, for any `packages/web` UI change. Resolve any finding both report. Both ran on 2026-09-08 and surfaced five real findings. The combined critique scored 30/40; the audit scored 18/20. The project owner chose to fix all five, including the tab row's keyboard model. That work spans capabilities beyond this change's own scope, so it moved to a new change, `studio-checks-signal-reach`. This task stays open here as a pointer. The new change carries its own verification task for the five fixes.
