## 1. The row model

- [x] 1.1 Rewrite the required-mark test in `packages/web/test/studio-formCardRows.test.ts` first. It reads `requirement` as `"required"` on the required entry and `"optional"` on the other. Verify it fails against today's code.
- [x] 1.2 Add two tests to the same file first, per the table in `design.md`. An entry whose `required` holds a CEL expression reads `"conditional"`, and `requiredCount` leaves it out. An entry declaring `required: false` reads `"optional"`. Verify both fail against today's code.
- [x] 1.3 Add assertions on `FORMS_LEGEND` to the same file first. Read the constant through `import * as cardRows from "../src/areas/studio/panels/formCardRows.js"`. A missing export then fails each named assertion, and the module still loads. The constant lists the five items in the order of `design.md`'s legend table. Each item names its catalog key and carries the sample entries that table gives. Verify the assertions fail against today's code.
- [x] 1.4 Replace `MiniatureEntry.required` with `requirement` in `panels/formCardRows.ts`, reading `isExpression` from `panels/shared/overrideMode.ts`. Export `FORMS_LEGEND` beside the row model. Add the six legend keys to `packages/web/src/i18n/catalogs/studio.ts`, with one comment over them. Switch the one reader in `FormsTab.tsx` to `requirement === "required"`. Rewrite the doc comments on `MiniatureEntry` and `requiredCount`. Verify typecheck passes, and 1.1 to 1.3 pass in the full suite run.

## 2. The dashed mark and the open control's height

- [x] 2.1 Add a check to `packages/web/test/studio-guidedSurfaceStyle.test.ts` first. The `miniatureConditional` block declares a dashed border style and `accentOnMuted` as its default border color. Its forced-colors border color reads `"CanvasText"`, with `forcedColorAdjust` at `"none"` there. The block declares no `backgroundColor` and reads no ramp step. Verify the check fails against today's code.
- [x] 2.2 Add a check to the same file first that the `openControl` block declares `minHeight: 24`. Verify it fails against today's code.
- [x] 2.3 Extend the compiled-class test in `packages/web/test/studio-formsTab.test.tsx` first. A view holds a required entry, a CEL-conditional entry and an entry declaring no `required`. The three marks carry three different compiled classes. Verify the test fails against today's code.
- [x] 2.4 Add the `miniatureConditional` style and the `MiniatureMark` component to `FormsTab.tsx`, per `design.md`. Render every mark and group break through `MiniatureMark`. Add `minHeight: 24` to `openControl`. Verify typecheck passes, and 2.1 to 2.3 pass in the full suite run.

## 3. The legend

- [x] 3.1 Narrow the `miniatures()` helper in `studio-formsTab.test.tsx` to the markup from the grid's label onward. Find that point by the attribute alone, `html.indexOf('aria-label="Forms in this process"')`, since the compiled `class` precedes it. Narrow the order test's `aria-hidden` lookup the same way. Verify both pass against today's code, since today's markup has no legend.
- [x] 3.2 Add legend markup assertions to the same file first. One `<ul>` carrying `role="list"` stands ahead of the grid, holding five `<li>` with the words of `design.md`'s table. Find it by its attribute alone, `aria-label="What the marks mean"`. Every sample group carries `aria-hidden="true"`, and each item's words sit outside it. The legend has no `<button`, no `<a ` and no `tabindex`. Verify these fail against today's code.
- [x] 3.3 Add an assertion to the same file that a draft whose steps declare no view renders no legend. It passes today and guards the rendering task below. Verify it passes against today's code.
- [x] 3.4 Add checks to `studio-guidedSurfaceStyle.test.ts` first. The `legend` block declares `display: "flex"`, `flexWrap: "wrap"` and no hidden overflow. Its gaps read `space.s4` and `space.s1`. The `legendItem` block declares `display: "flex"`, `alignItems: "flex-end"` and a `columnGap` of `space.s1`. The `legendSample` block declares `display: "flex"`, `alignItems: "flex-end"`, a `columnGap` of `space.s1` and `height: 24`. Verify the checks fail against today's code.
- [x] 3.5 Add a check to `studio-guidedSurfaceStyle.test.ts` that the `grid` block keeps `overflowY: "auto"`. It passes today and guards the rendering task below. Verify it passes against today's code.
- [x] 3.6 Render the legend ahead of the grid from `FORMS_LEGEND` and `MiniatureMark`, per `design.md`. Verify typecheck passes, and 3.2 to 3.5 pass in the full suite run.

## 4. The heading and the badge

- [x] 4.1 Rewrite the open-control naming test in `studio-formsTab.test.tsx` first. Its `aria-labelledby` second reference names an `<h2>` holding the step label. No `<span>` encloses that heading. Find the heading by its `id` attribute alone, since the compiled `class` may precede it. The pattern reads `<h2\b[^>]*\sid="REF"[^>]*>([^<]*)</h2>`, with the second reference in place of `REF`. Verify the test fails against today's code.
- [x] 4.2 Add a check to `studio-guidedSurfaceStyle.test.ts` first. The `name` block declares weight 800 and zero margin. It declares `fontFamily: fonts.body`, `fontSize: "inherit"`, `textTransform: "none"`, `letterSpacing: "normal"` and `color: colors.text`. Verify the check fails against today's code.
- [x] 4.3 Rewrite the badge tests in `studio-formsTab.test.tsx` first. Two issues read `aria-label="2 open issues on Intake"`, and one reads `aria-label="1 open issue on Intake"`. Two cards with one issue each carry names naming Intake and Review. A step label `$&` prints as `aria-label="1 open issue on $&amp;"`, since the markup escapes the ampersand. Rewrite the no-badge test to assert `not.toContain("open issue")`, paired with a one-issue render that contains it. Verify the name assertions fail against today's code.
- [x] 4.4 Make the step label an `<h2>` inside a `<div>` identity block. Add the resets `design.md` lists to `name`. Rewrite `formsTab.issueMark` and `formsTab.issueMarkOne` as whole sentences, with a comment naming both placeholders. Fill `{count}`, then `{step}` through a function, in `FormCard`. Verify typecheck passes, and 4.1 to 4.3 pass in the full suite run.

## 5. Documentation

- [x] 5.1 Extend the Form Card section of `DESIGN.md` with the legend, the dashed mark, the heading and the 24px minimum. State there that the label's `h2` keeps the body text at weight 800, apart from the Hierarchy's Title entry. Add the conditional mark to the Shapes section's list of dashed marks. Name the dashed mark in the Accent on Muted color entry, beside the filled one. Name the legend's samples on paper in that entry too, with the figures from `design.md`. Verify all three sections name what they gain.
- [x] 5.2 Rewrite the Forms tab paragraphs in `docs/current-state.md`. Name `FORMS_LEGEND`, `MiniatureMark`, the `requirement` field, the `<h2>` label and the badge's sentence. Verify every symbol they name exists after groups 1 to 4.
- [x] 5.3 Amend two steps of the Forms tab entry in `docs/browser-checks.md`. Step 1's pass line adds the legend on one line above the grid. The same line names the grid where it names the tab body. Step 4 adds fifteen field entries instead of ten. Re-read every later step that cites step 4. Verify each still holds.
- [x] 5.4 Append step 7 after step 6. The tester reads the legend's words, then its tree through `ariaSnapshot()`. Pass: a list named "What the marks mean" holds the five names and no sample node. Verify the step states its pass line.
- [x] 5.5 Extend step 7, where the tester resizes the window until the tab body measures about 400px tall. The tester reads three values before and after scrolling the grid to its end. Pass: the grid's `scrollTop` reads above 0 after the scroll. The tab body's own `scrollTop` reads 0 both times, and the legend's `getBoundingClientRect().top` holds its value. The step closes by restoring step 1's 635px body. Verify the step states its pass line.
- [x] 5.6 Append step 8. The tester reads the tab's heading outline, then the computed style of one card label. Pass: the process name stands at level 1 and each of the twelve labels at level 2. The label computes 15px, weight 800, the body's font family, no text transform and zero margin. Verify the step states its pass lines.
- [x] 5.7 Append step 9. The tester reads the bounding box of an open control. Pass: its height reads 24px or more. Verify the step states its pass line.
- [x] 5.8 Append step 10, where the tester opens "Prepare the Offboarding" and selects the "Forward to this address" entry. Under required, the tester opens the Developer view and picks CEL in its select. The tester presses "Edit as CEL" and types `data.email_forwarding == true`. Verify the step names that click path in order.
- [x] 5.9 Extend step 10 to a second entry. The tester authors the same condition on "M365 backup by Fabrikam as standard", a boolean that draws 8px. The tester returns without Save. Pass: the card draws two dashed marks in both schemes, and its foot still reads "16 fields". Verify the step states its pass line.
- [x] 5.10 Append step 11, which emulates forced colors through `run-code` and `page.emulateMedia` and clears the emulation at its end. Pass: the legend's four sample kinds stay visible and read apart. Step 10's two dashed marks stay visible too. Pass: the 8px dashed mark from step 10 reads apart from an ordinary 8px mark on another card. Where those two do not read apart, the tester records a new FORMS entry in `docs/decisions.md`. Verify the step states its pass lines.
- [x] 5.11 Name steps 6 and 10 as the two that leave the draft dirty, in the entry's closing paragraph. Add `forms-card-legend` tasks 5.3 to 5.10 to its Source line. Verify both lines read that way.
- [x] 5.12 Amend `.claude/rules/ui-glossary.md`, per `design.md`. The Forms tab row names the legend above the cards. The form card row names a heading where it names a label. Add the paragraph `design.md` quotes under the table, on the two uses of *section*. Verify the file has no new row and no second word for the legend.
- [x] 5.13 Delete the FORMS-2, FORMS-5, FORMS-6, FORMS-8, FORMS-9 and FORMS-13 bullets from `docs/decisions.md`. Keep every other tag. Append the two sentences `design.md` quotes to FIELDS-7. Re-read each `FormsTab.tsx:`, `formCardRows.ts:`, `studio.ts:`, `DESIGN.md:`, `current-state.md:`, `browser-checks.md:` and `design-language.md:` citation, with each bare `:NNN` continuation after one. Correct each line that no longer matches. Verify `grep -nE 'FORMS-(2|5|6|8|9|13)\b' docs/decisions.md` prints nothing.
- [x] 5.14 In `docs/decisions.md`, rewrite the Forms section's opening paragraph where it names the step name span. It names the card's heading there instead. Verify the paragraph names no span.

## 6. Verification

- [ ] 6.1 Run `bun run typecheck`, then `bun run build`, in the devcontainer. Verify both exit 0. Record what each printed.
- [ ] 6.2 Run the full `bun test` with `DATABASE_URL` set, piped through `scripts/gates/silent-green.sh`. Verify zero failures and a skip count at or under the floor.
- [ ] 6.3 Run `sh ~/.claude/skills/impeccable/scripts/impeccable detect --json packages/web/src/areas/studio/panels/FormsTab.tsx` once. Verify no finding stays open, or record why one stands.
- [ ] 6.4 Run the Forms tab browser check in full, new steps included, on the production build. Pass `-s=forms-card-legend` to every `playwright-cli` call. Read names and headings through `run-code` with `ariaSnapshot()`. Verify every step passes. Record the heights, names and foot texts read.
- [ ] 6.5 Run `/impeccable critique` on the Forms tab. Fix each finding, or record it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [ ] 6.6 Run `/impeccable audit` on the Forms tab. Fix each finding, or record it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [ ] 6.7 Repeat 5.13's citation sweep wherever a fix in 6.3 to 6.6 edited a file that `docs/decisions.md` cites. Verify each re-read citation matches its line, or record that no fix edited a cited file.
- [ ] 6.8 Run the prose gate over the pushed range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/prose.sh`. Verify it exits 0.
- [ ] 6.9 Run the whitespace gate over the same range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/whitespace.sh`. Verify it exits 0.
