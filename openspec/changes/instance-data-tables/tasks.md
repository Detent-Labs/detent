## 1. Storage

- [x] 1.1 Add a `reports` table (`instance_report_id`, `owner`, `process_id`, `name`, `query` jsonb — status/date-range/dataWhere — `columns` jsonb — ordered direct/merge column list, `created_at`, `updated_at`) to `src/engine/store.ts`'s schema init, and verify `bun run typecheck` and a fresh `initSchema` run against a clean test database both succeed.
- [x] 1.2 Add a `report_principals` join table (`instance_report_id` — `REFERENCES reports (instance_report_id) ON DELETE CASCADE`, `list` — `viewer` | `editor`, `principal` — an id, role or group string) with a composite primary key, and verify a duplicate `(instance_report_id, list, principal)` insert is rejected by the schema (a `bun:test` assertion).
- [x] 1.3 Add an index on `report_principals(principal, list)` for the "reports visible to me" listing, and verify it exists with a `pg_indexes` lookup by name (`bun:test`), matching the convention `migration.test.ts` and `instance-audit.test.ts` already use — not an `EXPLAIN`-parsing test, which no test in this repo does today.

## 2. Report CRUD (Runtime API Layer)

- [x] 2.1 Implement `createReport`, `updateReport`, `deleteReport`, `getReport` and `listMyReports` in `src/runtime/api.ts` (or a new `src/engine/reports.ts` sibling to `reporting.ts`, following that module's split), each taking an `Actor` and `db: SQL`, and verify each has a passing `bun:test` covering its own happy path, including a `bun:test` asserting `deleteReport` leaves no orphaned `report_principals` row behind (exercising the `ON DELETE CASCADE` from 1.2 directly, rather than trusting the constraint silently).
- [x] 2.2 Enforce on every write that the report's `owner` is always a member of its `editors` list — reject an update that would remove it — and verify with a `bun:test` asserting the rejection (spec: "The owner cannot be removed from editors").
- [x] 2.3 Enforce read/write authorization by `isEligibleCandidate`-style membership (owner or `editors` for write, owner/`editors`/`viewers` for read) against the calling `Actor`. Reuse `isEligibleCandidate` from `src/engine/transition.ts`, but first expand any group-shaped principal (recognizable by the `group_` prefix `src/auth/groups.ts:51` mints, no new column needed) via `getGroupMembers(groupId, db)` (`src/auth/groups.ts:116`) into its current member ids, since `isEligibleCandidate` itself matches only an id or a role and has no notion of a group. Verify with `bun:test` cases for an editor, a viewer, an unrelated actor, and an actor who belongs to a listed group but is not individually named (spec: "Report access is owner, editor or viewer, checked by membership").
- [x] 2.4 Verify saving a report never rejects on a `viewers`/`editors` entry lacking process `read` access (`bun:test`, spec: "Sharing with an actor lacking process access is not blocked").
- [x] 2.5 Add `getGroupsForMember(actorId, db)` to `src/auth/groups.ts` (`SELECT group_id FROM groups WHERE ${actorId} = ANY(members)`), the reverse of `getGroupMembers`, and have `listMyReports` expand its own match set to `[actor.id, ...actor.roles, ...groupIds]` before querying `report_principals(principal, list)`, so a report shared only through a group the actor belongs to appears in their list. Verify with a `bun:test`: a report naming only a group in `viewers` appears in `listMyReports` for a member of that group who is named nowhere else on the report.

## 3. Report execution

- [x] 3.1 Export `buildInstanceWhere` and `buildDataWhere` from `src/runtime/api.ts` (currently module-private, `api.ts:1304` and `api.ts:1362`), with no behavior change, so the report-execution code below builds the exact same instance predicate `queryInstances` uses rather than a second, drift-prone copy of it.
- [x] 3.2 Implement `executeReport(reportId, actor, db)` reusing `queryInstances`'s `InstanceQueryFilter` shape for the report's stored query, and verify a `bun:test` that a saved report's query returns the same instances an equivalent direct `queryInstances` call would.
- [x] 3.3 Gate execution on both the report's own owner/editor/viewer membership AND `can(actor, "read", processId, db)` from `src/auth/authorize.ts`, returning an empty table (not a refusal) when membership passes but the process `read` check fails, and verify both `bun:test` scenarios in the spec ("Report sharing narrows access, never widens it").
- [x] 3.4 Implement column resolution: using the exported `buildInstanceWhere`/`buildDataWhere` from 3.1, run a `DISTINCT version` query under the report's own filter to find the `(processId, version)` pairs actually matched, resolve each version's field catalog through a fresh `createDefinitionStore` (per-execution cache, not `listInstances`'s persistent one), and return the union — built from `leafFields` (`src/schema/definition.ts:361`), not the raw field tree, since a `type: "group"` container carries no value of its own — tagged with which versions declare each field; verify with a `bun:test` covering a field only an older in-range version declares, and one confirming a group container field is never offered as a column choice (spec: "Column choices come from the union of field catalogs in range").
- [x] 3.5 Compute the three-way empty-cell state per direct-field column: redacted first, whenever `instance.redactedAt` is set (applies to every field on that instance uniformly — `redactInstance` wipes `data` wholesale, so this check does NOT gate on the field's own `redactable` flag); otherwise not-in-this-version from the resolved version's catalog; otherwise no-value from `data[fieldId]`. Verify one `bun:test` scenario per state, plus one confirming a field with `redactable: false` still reads as redacted on a redacted instance (spec: "A cell distinguishes no-value, not-yet-existing and redacted").
- [x] 3.6 Compute merge-column values (first non-empty source in order; concatenate and flag a collision when more than one source has a value; result typed and sorted as text) and a per-column collision count. Check `instance.redactedAt` first, same priority as 3.5's direct-field rule: a redacted instance's merge cell renders redacted, not as an ordinary empty value, even though every source field reads empty post-redaction. Verify with `bun:test` covering no-collision, collision, the reported count, and the redacted case (spec: "A merge column takes the first non-empty source value and marks a collision").
- [x] 3.7 Surface a `truncated` flag on the execution result mirroring `queryInstances`'s own bounding, and verify with a `bun:test` exceeding the bound (spec: "Report execution reuses the instance-query filter axes and its bounding").

## 4. HTTP routes

- [x] 4.1 Add report CRUD routes (`GET/POST /reporting/reports`, `GET/PUT/DELETE /reporting/reports/:id`) to `src/http/reporting-routes.ts`, register each in `src/http/server.ts`'s route table following the `seg(...)` pattern the existing `/reporting/*` entries use (`server.ts:669-676`), gated by `REPORTS_ROLE` at the route and the Runtime API Layer's own membership checks per-report, update `reporting-routes.ts`'s header comment (currently "Read-only throughout: no handler here writes") to state that only the three view handlers stay read-only and the report routes are the documented exception, and verify with `bun:test` HTTP-level tests for each verb, including a case asserting a request without `REPORTS_ROLE` is refused with `403`.
- [x] 4.2 Add the execution route (`GET /reporting/reports/:id/table`) and the column-choice read the builder uses before a report is saved (an unsaved-draft execution, same shape), and register both in `src/http/server.ts`'s route table alongside the CRUD routes from 4.1. Gate the draft/column-choice preview with `can(actor, "read", processId, db)`, the same check task 3.3 applies to saved-report execution, returning an empty result rather than real field values when it fails. Verify with `bun:test` covering both a saved report and an unsaved draft configuration, including a case asserting an actor without process `read` access gets no real data from the preview (spec: "Previewing an unsaved draft requires the same process read permission").
- [x] 4.3 Verify a request naming an unknown report id returns `404` and a malformed query/column body returns `400` with no query run (`bun:test`, matching `reporting-analytics-api`'s existing error-shape convention).

## 5. Frontend: report builder screen

- [x] 5.1 Run `/frontend-design:frontend-design` for visual direction before implementing any screen below, per the repo convention (`CLAUDE.md`); pull in `web-design-guidelines`, `vercel-react-best-practices` and `vercel-composition-patterns` for the UI/UX pass — matching `add-reporting-analytics` task 5.2, the change that built this same area.
- [x] 5.2 Add a report-builder route and screen under `packages/web/src/areas/reporting`, gated the same way the area's other three views are (login redirect, missing-role refusal), and verify manually in a browser per `docs/browser-checks.md`'s convention plus a component-level check of the pure view-model pieces only (components themselves stay untested, per the reporting-app convention).
- [x] 5.3 Build the process/filter/column picker, showing per-column version coverage for a field not common to every in-range version, and verify against a seeded process with two versions in the devcontainer's Postgres.
- [x] 5.4 Build the merge-column editor (ordered source-field picker, collision count display), and verify against a seeded pair of instances producing a collision.
- [x] 5.5 Build save/name/reopen/edit for a report, and the reports-list screen backing "reopen a report you own or can edit," and verify a save-then-reopen round trip in a real browser.
- [x] 5.6 Build the viewer/editor share dialog (add/remove by id, role or group; owner-removal blocked in the UI; a hint shown for a name lacking process `read` access), and verify both UI behaviors in a real browser.
- [x] 5.7 Update `packages/web/test/reporting-boundaries.test.ts`'s "no mutating HTTP method is issued" assertion to permit the report-builder's writes while still enforcing the narrowed rule: every mutating request the area's client issues targets a `/reporting/reports` route, and the three existing views' own requests stay read-only. This closes out the `reporting-app` MODIFIED requirement (see `proposal.md`, `specs/reporting-app/spec.md`).

## 6. Frontend: table rendering

- [x] 6.1 Render the table with the three empty-cell states and merge collisions as visibly distinct (not just distinct data), following `.claude/rules/design-language.md`, and verify visually in a real browser against seeded data exercising all three empty states plus a collision.
- [x] 6.2 Render the truncated-result notice in words, matching the existing reporting views' "empty result is stated in words" convention, and verify in a real browser against a result exceeding the bound.

## 7. Localization

- [x] 7.1 Add an English and German catalog for every new string (headings, column picker, merge editor, save/share dialog, empty-cell labels, truncation notice) with matching key sets, and verify with a `bun:test` asserting key parity, matching `reporting-app`'s existing catalog test.

## 8. Verification

- [x] 8.1 Run `bun run typecheck` and report the output.
- [x] 8.2 Run the full `bun test` suite with `DATABASE_URL` set (never a single-file rerun) and report the pass count and skip count, confirming no DB-backed suite skipped silently.
- [x] 8.3 Run a real-browser check of the full report-builder flow (build, save, share, view, including a truncated and a redacted-cell case) per `docs/browser-checks.md`'s convention, and report what was exercised.
- [x] 8.4 Mark the "Instance data tables" entry in `docs/decisions.md` shipped, following the convention `process-read-permission` task 5.3 and `instance-query-core` task 6.4 each used for their own entries.
