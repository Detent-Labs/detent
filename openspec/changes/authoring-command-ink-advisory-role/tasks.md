## 1. The advisory role in the token files

- [x] 1.1 Add a `describe` block for the advisory role to `packages/web/test/studio-guidedSurfaceStyle.test.ts` first. The `:root` block of `tokens.css` declares `--advisory-500: #e25a40;` and `--color-advisory: var(--advisory-500);`. The dark block declares `--advisory-500: #ff9783;` and no `--color-advisory`. The token module holds `advisory500: "var(--advisory-500)"` and `advisory: "var(--color-advisory)"`. Verify the checks fail against today's code.
- [x] 1.2 Add a contrast check to the same block first. It parses `--paper-50`, `--ledger-100` and `--advisory-500` from both blocks of `tokens.css`. It reads each value as `token()` in `studio-fieldMatrixBadge.test.ts` does: the first declaration is light, the last dark. It applies the WCAG 2 formula from `design.md`. The role measures at least 3:1 against paper and against ledger in each scheme. Verify the check fails against today's code.
- [x] 1.3 Add the primitive, its dark override and the alias to `packages/web/src/shell/tokens.css`, per `design.md`. Above the primitive, write a comment stating 3.26:1 on paper and 3.00:1 on ledger. The comment also states that the value is no step of the accent ramp. Add `advisory500` and `advisory` to `packages/form-ui/src/tokens.stylex.ts`. Set that module's header count to 44. Verify `grep -cE '^ +[a-zA-Z0-9]+: "var\(' packages/form-ui/src/tokens.stylex.ts` prints 44.
- [x] 1.4 Run `bun install` in the devcontainer, so `packages/web` reads the new token module. Without that step, web typecheck reports TS2339 on `colors.advisory` while the build passes. Verify the install exits 0, typecheck passes, and 1.1 and 1.2 pass in the full suite run.

## 2. Every advisory reader takes the role

- [x] 2.1 Add checks to the advisory role's block first. Each of the eleven blocks in the context table of `design.md` reads `colors.advisory` for its border color. Read each through `styleBlock`, on its source with comments stripped. Verify every check fails against today's code.
- [x] 2.2 Add a walk over `packages/web/src` and `packages/form-ui/src` to the same block first. It strips comments from each `.ts` and `.tsx` module. It also finds every `colors.advisory` read, and each sits on a `border…Color` key. No module names `colors.accent400` afterwards. Its failure message names each file it found. Verify the check fails against today's code, naming eleven files.
- [x] 2.3 Point the eleven blocks at `colors.advisory`. Rewrite the comment above `cardEmpty` in `FormsTab.tsx`, so it names the advisory role in place of the checks rail's dot. Verify typecheck passes, and 2.1 and 2.2 pass in the full suite run.
- [x] 2.4 Rewrite the comment above the `miniatureRequired` assertion in `studio-guidedSurfaceStyle.test.ts`. Drop its sentence saying that `cardEmpty` reads `accent400`. Verify the comment names no read of `accent400`.

## 3. The authoring command's ink text

- [x] 3.1 Add a `describe` block for the authoring command to `studio-guidedSurfaceStyle.test.ts` first. It reads `openControl` in `FormsTab.tsx`, `control` in `FormTabStrip.tsx` and `command` in `ChangeList.tsx`. Each block's `color` reads `colors.textMuted` by default, and `colors.text` under `":hover"` and `":active"`. Each block keeps its `surfaceMuted` hover wash and its ink-14% press wash. Verify the checks fail against today's code.
- [x] 3.2 Add a check on the `controlDisabled` block of `FormTabStrip.tsx` to the same `describe` first. Its `color` reads `colors.textMuted` for the default, `":hover"` and `":active"`. Its `backgroundColor` reads `"transparent"` for all three. Verify the check fails against today's code.
- [x] 3.3 Add a contrast check to the same `describe`. It parses paper, ledger and ink the way 1.2 does. Ink measures at least 4.5:1 against ledger in each scheme. Ink also measures at least 4.5:1 against the press wash, ink at 0.14 over paper. Verify the check passes against today's code, since it reads token values alone.
- [x] 3.4 Add a markup test to `packages/web/test/studio-formTabStrip.test.tsx` first. It finds each control by its text. With the first tab open, only "Move left" carries the `controlDisabled` class. With the last tab open, only "Move right" carries it. Verify the test fails against today's code.
- [x] 3.5 Add the conditional `color` to `openControl`, `control` and `command`, per `design.md`. Add the `controlDisabled` block to `FormTabStrip.tsx`. Write a comment above it: StyleX 0.19 orders `:disabled` before `:hover`, so the block stacks. Stack it after `control` on each move control, from the boolean that sets `disabled`. Let the strip's local `ghost` helper take its styles as separate arguments. Verify typecheck passes, and 3.1, 3.2 and 3.4 pass in the full suite run.
- [x] 3.6 Extend the comment above each of `openControl`, `control` and `command` with the ink text on hover and on press. Keep every sentence already in each comment. Verify each of the three comments names the ink text and keeps its earlier sentences.

## 4. Documentation

- [x] 4.1 In the front matter of `DESIGN.md`, add `advisory-500: "#e25a40"` to `colors`. Add `button-authoring-hover` to `components`, with `backgroundColor: "{colors.ledger-100}"` and `textColor: "{colors.ink-900}"`. Add `button-authoring-active` after it, with `textColor: "{colors.ink-900}"`. Verify each new entry sits at its neighbors' indent.
- [x] 4.2 Rewrite the Advisory entry under Secondary in `DESIGN.md`. It states `#e25a40` light and `#ff9783` dark, with 3.26:1 on light paper. It names the warning callout's rule and the empty form card's box. It also names the dashed boxes of an incomplete condition and an unresolved migration mapping. Delete its clause naming a check result, and its sentence on the accent ramp's missing dark override. Verify the entry names both values and all four marks, and no check result.
- [x] 4.3 In the Role Rule of `DESIGN.md`, replace "Advisory still needs a role there." with a sentence naming `colors.advisory`. Verify `grep -n 'still needs a role' DESIGN.md` prints nothing.
- [x] 4.4 Extend the authoring command line in the Buttons list of `DESIGN.md`. Hover and press both turn the text to ink. A disabled command keeps slate text and a transparent ground under the pointer. Verify the line names the ink text and the disabled look.
- [x] 4.5 In the Form Card and Warning Callout sections of `DESIGN.md`, name the Advisory role where each names the advisory tone. Verify `grep -n 'advisory tone' DESIGN.md` prints nothing.
- [x] 4.6 In the Color section of `.claude/rules/design-language.md`, add a sentence after the dormant role's. The advisory tone reads `--color-advisory` (`colors.advisory`), backed by the `--advisory-500` primitive. It draws a rule or a border, and never a stamp. Verify the section names the role.
- [x] 4.7 In the same file, extend the authoring command paragraph. Under the pointer and while pressed, the command's text turns to ink over its wash. A disabled command keeps slate text and a transparent ground under the pointer. Verify the paragraph names the ink text and the disabled look.
- [x] 4.8 In the same file, rewrite the warning callout paragraph, so its rule reads the advisory role. Delete the phrase naming the accent ramp's light step. Delete the sentence saying the tone has no role in `tokens.css`. Verify `grep -n 'has no role' .claude/rules/design-language.md` prints nothing.
- [x] 4.9 In the Forms tab paragraphs of `docs/current-state.md`, name `colors.advisory` where the empty form's box stands. Verify that sentence names the role.
- [x] 4.10 Append a step to the Forms tab card entry in `docs/browser-checks.md`, after its last numbered step. The tester hovers one card's open control and reads its computed `color` and `backgroundColor`. The tester then holds it down through `page.mouse.down()` and reads both again. After the reads, the tester moves the pointer off the control with `page.mouse.move`, then calls `page.mouse.up()`. The tester repeats the hover, the press and the release in the other color scheme. Verify the step moves the pointer off before each release.
- [x] 4.11 State that step's pass lines. Pass: the text reads `rgb(32, 30, 29)` light and `rgb(243, 242, 242)` dark, both times. Pass: the hover ground reads `rgb(234, 233, 233)` light and `rgb(45, 43, 43)` dark, and the press ground reads ink at alpha 0.14. Pass: the Forms tab stays open after each release. Verify the step states all three pass lines.
- [x] 4.12 Extend that step to the card that the entry's removal step empties, "Hold and Close the Account". Where that card holds fields again, repeat the removal first. The tester reads the card's computed border color in both schemes. Pass: it reads `rgb(226, 90, 64)` light and `rgb(255, 151, 131)` dark. Add this change's tasks 4.10 to 4.12 to the entry's Source line. Verify the step states its pass line, and the Source line names the three tasks.
- [x] 4.13 In the tabbed step form entry of `docs/browser-checks.md`, add a paragraph after the add-and-remove paragraph of "The author's strip". Open it with "From `authoring-command-ink-advisory-role` tasks 4.13 to 4.15." The tester hovers "Add a tab", then holds it down, in both schemes. After each read, the tester moves the pointer off the control with `page.mouse.move`, then calls `page.mouse.up()`. Verify the paragraph moves the pointer off before each release.
- [x] 4.14 State that paragraph's pass lines. Pass: "Add a tab" reads ink text over the ledger wash, then over the press wash. Pass: the strip still holds two tabs after each release. Verify the paragraph states both pass lines.
- [x] 4.15 Extend that paragraph with the first tab open. The tester chooses "Review" first, so the first tab stands open. The tester then hovers "Move left", which stands disabled there. Pass: its text stays slate, `rgb(96, 93, 93)` light and `rgb(155, 151, 151)` dark. Pass: its ground stays transparent. Verify the paragraph states both pass lines.
- [x] 4.16 Add an entry to `docs/browser-checks.md` after the Forms tab card entry, headed "The advisory role and the change list's command (`authoring-command-ink-advisory-role`)". Its Source line names this change's tasks 4.16 to 4.19. Its setup opens a fresh `it_offboarding` draft on the production build, as the Forms tab card entry does. Its closing paragraph discards the draft from the process list, as that entry's closing paragraph does. Verify the entry states its Source line, setup and closing paragraph.
- [x] 4.17 Add the entry's first step. The tester types `de` into the add-locale control of the header bar's `⋮` menu. The tester then chooses "+ add locale". Pass: the warning callout under the process name draws its 3px rule in `rgb(226, 90, 64)` light and `rgb(255, 151, 131)` dark. Verify the step states its pass line.
- [x] 4.18 Add the entry's second step. The tester opens the Steps tab and chooses "Submit the Exit Notification" in the steps rail. In that step page's "Path to" section, under the first path's "triggered by", the tester chooses "a condition". Under "Only when", the tester chooses "+ Add row" and leaves the row's value empty. Pass: the row's dashed border reads the same two colors as the first step. Verify the step states its pass line.
- [x] 4.19 Add the entry's third step: the tester opens the Changes tab, which lists the path the second step changed. The tester then hovers "Expand all" and holds it down, in both schemes. After each read, the tester moves the pointer off the control with `page.mouse.move`, then calls `page.mouse.up()`. Pass: its text reads ink both times, in the values of the Forms tab card entry. Pass: every change row stays folded after each release. Verify the step states both pass lines.
- [x] 4.20 Add a section `## Open from the authoring-command-ink-advisory-role review (each needs its own OpenSpec change)` to `docs/decisions.md`, after the Forms tab audits section and before `## Refused simplifications`. Its opening paragraph names this change's 2026-09-13 review as the source. It also states that the TONE tags are local to it, and that paths under `panels/` and `screens/` start at `packages/web/src/areas/studio/`. Verify the heading and both sentences appear, with no bullet yet.
- [x] 4.21 Add the bullet TONE-1 to that section. It records that the step page draws `missingTranslationWarning` beside a 2px refusal rule. The header bar, the field catalog and the form editor draw the same message beside the 3px advisory rule. It records two more 3px rules mixing refusal at 55%, in the checks rail and the timers panel. Cite each block by file and line, and state the bullet's risk, as the field-matrix-fill-height section's bullets do. Verify `grep -n 'TONE-1' docs/decisions.md` prints the bullet.
- [x] 4.22 Delete the FORMS-7 and FORMS-14 bullets from `docs/decisions.md`. Keep every other tag. Verify `grep -nE 'FORMS-(7|14)\b' docs/decisions.md` prints nothing.
- [x] 4.23 Re-read each citation left in the Forms tab audits section naming `FormsTab.tsx:`, `FormTabStrip.tsx:`, `ChangeList.tsx:`, `DESIGN.md:`, `design-language.md:`, `current-state.md:` or `browser-checks.md:`. Re-read each `DESIGN.md:` or `tokens.css:` citation elsewhere in `docs/decisions.md` too. Include each bare `:NNN` continuation after one. Correct each line that no longer matches. Verify each re-read citation matches its line.

## 5. Verification

- [x] 5.1 Run `bun run typecheck`, then `bun run build`, in the devcontainer. Verify both exit 0. Record what each printed.
- [x] 5.2 Run the full `bun test` with `DATABASE_URL` set, piped through `scripts/gates/silent-green.sh`. Verify zero failures and a skip count at or under the floor.
- [x] 5.3 Run `sh ~/.claude/skills/impeccable/scripts/impeccable detect --json` on each of the thirteen component files the Impact section of `proposal.md` lists. Verify no finding stays open, or record why one stands.
- [x] 5.4 Run the three browser check entries this change touched, on the production build. Pass `-s=authoring-command-ink-advisory-role` to every `playwright-cli` call. Read computed styles through `run-code` with `page.evaluate`. Switch the scheme through `page.emulateMedia`. Verify every new pass line holds, and record each color read.
- [x] 5.5 Run `/impeccable critique` on the Forms tab, the form editor and the Changes tab. Run it on a step page showing an incomplete condition too. Run it on the Fields tab with the `de` locale added, where a field's warning callout meets the current mark. Fix each finding, or record it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [x] 5.6 Run `/impeccable audit` on each screen that 5.5 names. Fix each finding, or record it in `docs/decisions.md`. Verify no finding stays unaddressed.
- [x] 5.7 Wherever a fix in 5.3 to 5.6 touched a file, re-read every citation of that file anywhere in `docs/decisions.md`. Read each bare `:NNN` continuation after one too. Correct each line that no longer matches. Verify each re-read citation matches its line, or record that no fix touched a cited file.
- [ ] 5.8 Run the prose gate over the pushed range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/prose.sh`. Verify it exits 0.
- [ ] 5.9 Run the whitespace gate over the same range: `sh scripts/gates/range.sh < /dev/null | sh scripts/gates/whitespace.sh`. Verify it exits 0.
